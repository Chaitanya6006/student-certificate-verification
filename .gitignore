# Dependencies
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Build output
dist/
build/

# Environment variables
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# Contract artifacts (generated by `compact compile`)
contracts/managed/

# Deployment info
deployment.json

# Wallet seeds (sensitive)

# OS generated files
.DS_Store
Thumbs.db

# IDE files
.vscode/settings.json
.idea/
*.swp
*.swo

# Logs
logs/
*.log

# Runtime data
pids/
*.pid
*.seed
*.pid.lock

# Coverage directory
coverage/

# Private state
*-state/

# Temporary files
tmp/
temp/

# Midnight network state (per-network seeds, deploy addresses — never commit)
.midnight-state.json

# Midnight wallet sync state (per-network serialized wallet state cache)
.midnight-wallet-state/

# Frontend
frontend/node_modules/
frontend/dist/
frontend/.vite/
frontend/.env.local

# Frontend build artifacts copied by `npm run frontend:keys` (compiled contract + ZK keys)
frontend/src/generated/
frontend/public/managed/

# Admin secret for the certificate contract (sensitive — never commit)
.certificate-admin-secret.json

# LevelDB private-state store
midnight-level-db/
